// Point masses in system

// Number to expect
num_point_masses = 1;

// Properties of each point mass.
point_masses = ( {	// Body properties
					mass = 1.0;
					radius = 0.0;
					
					// Orbit properties
					semi-major_axis = 7.0;		// Size of semi-major axis of orbit (CL)
					eccentricity = 0.0;			// Eccentricity of orbit
					inclination = 0.0;			// Inclination of orbit
					long_asc_node = 0.0;		// Longitude of ascending node
					arg_periapsis = 0.0;		// Argument of periapsis
					mean_anom = 0.0;			// Mean anomaly
					
					// Drift rates
					inv_axis_drift = 0.0;		// Inverse of semi-major axis drift rate (1/CT)
					inv_e_drift = 0.0;			// Inverse of ellipticity drift rate (1/CT)
					drift_fold_time = 0.0;		// Exponential timescale for reduction of drift (CT)
					 }
				);